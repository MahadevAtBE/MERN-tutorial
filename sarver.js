console.log("hello server");

// to make a npm project we write on terminal npm init 
// now fill package name,version(escapeble),description,entry point(sarver.js),git reposetry(esc),keyword,author
// we can install packages in node like npm install (package name)/ npm i (package name) 
