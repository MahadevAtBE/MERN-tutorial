import React from 'react'

const page = () => {
  return (
    <div>
    logout page
    </div>
  )
}

export default page

// (admin) -> this is called grooping
// it mens we can acces its childres pages in brouser wethout writing /admin/ on 
// we can access by just typing chidren name like adminlogout
